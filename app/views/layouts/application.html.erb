<!DOCTYPE html>
<html>
<head>
  <title>WordFrequencyComparator</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_pack_tag 'application' %>
  <% console %>
</head>

<body>
<div class="header">
<div>
<% if flash.any? %>
  <% flash.each do |type,msg| %>
    <span class="<%= type %>">
      <%= msg %>
    </span>
  <% end %>
<% end %>
</div>
<div class='login'>
<% if logged_in? %>
  <span class='blue'>Logged in as <%= current_user.username %> | </span>
  <%= link_to "Logout", logout_path, method: :delete, class: "link" %> 
<% end %>
</div>
<div class="title">Word<span class="yellow">Frequency</span><span class="green">Comparator</span></div>
</div>
</div>
<%= yield %>
</body>
</html>
