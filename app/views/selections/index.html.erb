<p id="notice"><%= notice %></p>

<h1>Selections</h1>

<table>
  <thead>
    <tr>
      <th>New</th>
      <th>Create</th>
      <th>Show</th>
      <th>Index</th>
      <th>Destroy</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @selections.each do |selection| %>
      <tr>
        <td><%= selection.new %></td>
        <td><%= selection.create %></td>
        <td><%= selection.show %></td>
        <td><%= selection.index %></td>
        <td><%= selection.destroy %></td>
        <td><%= link_to 'Show', selection %></td>
        <td><%= link_to 'Edit', edit_selection_path(selection) %></td>
        <td><%= link_to 'Destroy', selection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Selection', new_selection_path %>
